--- a/sqlite3/query.go
+++ b/sqlite3/query.go
@@ -102,7 +102,7 @@ func makePlaceHolders(n int) string {
 
 	if len(filter.Kinds) > 0 {
-		if len(filter.Kinds) > 10 {
+		if len(filter.Kinds) > b.QueryKindsLimit {
 			// too many kinds, fail everything
 			return "", nil, TooManyKinds
 		}
